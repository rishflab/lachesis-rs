syntax = "proto3";

message Transaction {
  string message = 1;
}

message TransactionReceipt {
  string message = 1;
}

service Client {
  rpc SendTransaction(Transaction) returns (TransactionReceipt);
}